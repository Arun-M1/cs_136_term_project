This package keeps each algorithm file aligned with the original term_project.c logic as closely as possible.
Only the main functions and command-line handling were changed so each algorithm can be run separately.
